# DONE.eslint — react-router-msal-google-starter-code

Lint debt resolved.
Resolved 2026-08-23 — 9 findings cleared. oxlint runs via `npx oxlint .` (`npm run lint`).
Dispositions below; a follow-up pass may convert config disables back into source fixes.

| Rule                     | Count | Disposition                  |
| ------------------------ | ----- | ---------------------------- |
| `eslint(no-unused-vars)` | 9     | disabled in `.oxlintrc.json` |
